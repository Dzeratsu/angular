export interface transportGroup {
  id: number,
  name: string
  description: string
  unitID: number[]
  }

export interface addGroupResp {
  id:number
}

export interface allTransport{
  id: number;
  name: string;
  description: string,
  unitID: number[]
}

